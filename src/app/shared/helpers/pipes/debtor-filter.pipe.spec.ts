import { DebtorFilterPipe } from './debtor-filter.pipe';

describe('DebtorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DebtorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
